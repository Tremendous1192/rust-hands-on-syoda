fn main() {
    // Declare and initialize variables
    let x = 100;
    let y: i64 = 200;

    // Perform addition
    let z = x + y;

    // Print the result
    println!("{} + {} = {}", x, y, z);
}
