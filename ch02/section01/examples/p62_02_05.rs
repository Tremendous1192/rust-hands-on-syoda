fn main() {
    // Declare and initialize constant variables
    const X: i32 = 123;
    const Y: i32 = 45;

    // Perform addition and print the result
    const Z: i32 = X + Y;
    println!("{} + {} = {}", X, Y, Z);
}
