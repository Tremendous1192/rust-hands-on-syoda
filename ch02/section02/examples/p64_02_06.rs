fn main() {
    let num = 1234; // Declare and initialize the variable 'num'

    // Check if 'num' is even or odd and print the result
    if num % 2 == 0 {
        println!("{} is an even number.", num);
    } else {
        println!("{} is an odd number.", num);
    }
}
